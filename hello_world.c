#include <stdio.h>

int main() {
    int car;
    printf("int: ");
    scanf("%d", &car);
    printf("%d", car);
    if (car < 0) {
        printf("bababa");
    }
    else {
        printf("buobadusbjk");
    }
return 0;
}
